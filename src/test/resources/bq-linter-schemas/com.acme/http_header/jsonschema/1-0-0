{
    "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
    "description": "Schema whose only lint issues come from the BigQuery linters",
    "self": {
        "vendor": "com.acme",
        "name": "http_header",
        "format": "jsonschema",
        "version": "1-0-0"
    },

    "type": "object",
    "properties": {
        "x-acme-header": {
            "description": "A header name BigQuery would not accept as a column name",
            "type": "string",
            "maxLength": 256
        }
    },
    "additionalProperties": false
}
